<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="CustomChip" parent="Widget.MaterialComponents.Chip.Choice">
        <item name="android:textColor">@color/custom_colors</item>
        <item name="chipStrokeColor">@color/custom_colors</item>
        <item name="chipStrokeWidth">2dp</item>
        <item name="chipCornerRadius">8dp</item>
        <item name="chipBackgroundColor">@color/black_light</item>
    </style>

    <style name="CustomRadioButton" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:button">@null</item>
        <item name="android:textColor">@android:color/transparent</item>
        <item name="android:layout_marginStart">12dp</item>
    </style>

    <style name="BottomSheetTheme" parent="Theme.MaterialComponents.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/background_filter</item>
    </style>

    <style name="TextColor">
        <item name="android:textColor">@color/text_blue</item>
    </style>

</resources>